{
  "name": "sprint90",
  "version": "0.1.0",
  "description": "AI-Driven JEE & CBSE Exam Prep Platform",
  "workspaces": [
    "backend",
    "frontend",
    "admin-frontend"
  ],
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:admin": "cd admin-frontend && npm run dev",
    "dev:all": "concurrently 'npm run dev:backend' 'npm run dev:frontend' 'npm run dev:admin'",
    "build:all": "npm run build -w backend && npm run build -w frontend && npm run build -w admin-frontend",
    "install-all": "npm install && npm install -w backend && npm install -w frontend && npm install -w admin-frontend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}